import { Meta, Canvas, Story, Controls } from '@storybook/addon-docs';
import stories from './button.stories'

<Meta of={stories} />

# Button

The Button component can be configured to use many different colors. However,
the age group colors should only ever be used with the `contained` variant to
maintain proper legibility.

<Canvas>
  <Story />
</Canvas>

<Controls />

